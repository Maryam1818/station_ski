version: "3"
services:
  spring-ski:
    image: maryamgadri/ski_image
    ports:
      - "8089:8089"
    networks:
      - skii
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysqldb/stationSki
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
    depends_on:
      - mysqldb

  mysqldb:
    image: mysql:8
    volumes:
      -  /home/mysql/data:/var/lib/mysql:rw
    networks:
      - skii
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=stationSki

networks:
  skii: